var gulp    = require('gulp');

gulp.task('test', function () {
 
});

/**gulp.task('default', function() {
  gulp.src([])
    .pipe(karma({
      configFile: 'karma.conf.js',
      action: 'watch'
    }));
});
**/
